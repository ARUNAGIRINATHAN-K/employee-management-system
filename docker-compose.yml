services:
  employee-management:
    build:
      context: .
      dockerfile: Dockerfile
    image: employee-management:latest
    ports:
      - "8080:8080"
    environment:
      SERVER_PORT: 8080
      APP_UPLOAD_DIR: /app/uploads
    volumes:
      - ./uploads:/app/uploads